# 2026-06-10 工作日志

## 画笔打码 UI 隐藏（MVP 上线前）
- **原因**：画笔模式 bug 较多，先隐藏 UI 切换入口，上架 MVP 版本
- **改动**：`MosaicModule.getOptionsBarHTML()` 移除绘制模式切换按钮和画笔大小滑块，仅保留效果类型（马赛克/模糊）下拉 + 对应参数滑块
- 画笔代码全部保留（`_startBrush`/`_continueBrush`/`_finishBrush`/`_applyBrushMaskedEffect` 等），后续修好 bug 后恢复 UI 即可
- 涉及文件：`src/modules/MosaicModule.js`
